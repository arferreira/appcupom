<%= @user.name %>,

Para recuperar sua senha acesse o link abaixo dentro de duas horas:

<% if Rails.env.production? %>
<%= "http://www.trazcupom.com/password_resets/#{@user.password_reset_token}/edit" %>
<% else %>
<%= "http://www.trazcupom.com/password_resets/#{@user.password_reset_token}/edit" %>
<% end %>

Se você não pediu para recuperar a senha, ignore este e-mail que sua senha continua a mesma.
