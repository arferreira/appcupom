<div id="main-container" class="scrollable view-profile-establishment">
  <div id="main" class="clearfix">
    <div class="wrapper">
      <header>
        <h1 class="static-title">Perguntas Frequentes</h1>
      </header>
		<p>Reunimos aqui perguntas e respostas para as dúvidas mais frequentes. Caso não encontre a resposta que procura entre em contato através do Fale Conosco.</p><br />
      <%#Aqui verifico se é Usuario %>
      <% if user_type == USER_TYPE %>
        <% @faqsU.each do |faq| %>
          <tr>
          <article>
            <h3><%= faq.question %></h3>
            <p><%= faq.answer %></p>
          </article>
        <% end %>
      <% end %>

      <%#Aqui verifico se é Parceiro %>
      <% if user_type == PARTNER_TYPE %>
        <% @faqsP.each do |faq| %>
          <tr>
          <article>
            <h3><%= faq.question %></h3>
            <p><%= faq.answer %></p>
          </article>
        <% end %>
      <% end %>
      
      <%#Aqui verifico se é Admin %>
      <% if user_type == ADMIN_TYPE %>
        <% @faqsA.each do |faq| %>
          <tr>
          <article>
            <h3><%= faq.question %></h3>
            <p><%= faq.answer %></p>
            <div><%= faq.type %></div>
            <div class="cell"><%= button_to 'New Faq', new_faq_path %></div>
            <div class="cell"><%= button_to 'Show', faq %></div>
            <div class="cell"><%= button_to 'Edit', edit_faq_path(faq) %></div>
            <div class="cell"> <%= button_to 'Destroy', faq, confirm: 'Are you sure?', :method => :delete, :class => :destroy  %></div>
          </article>
        <% end %>
      <% end %>
      <br />
    </div>
  </div>
</div>

<div id="boxes">
  <!-- Não remova o div#mask, pois ele é necessário para preencher toda a janela -->
  <div id="mask" ontouchmove="blockMove(event)"></div>
</div>
