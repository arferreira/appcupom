<div id="comment-chat-body" >
	
	<div id="comment-chat-tx-area" <% if signed_in? USER_TYPE %>>
	<%= form_for sPartnerComment.new, :remote => true do |f| %>
		<%= f.text_area :comment, :rows => 3, :cols => 70 	 	%><br />
		<%= f.hidden_field :partner_id, :value => @partner.id %>
		<div class="but-submit">
		<%= f.submit "Enviar", :class => "title-box-shadow" %>
		</div>
	<% end %>
	</div>
	<% end %>
	
	<div id="comment-notice"></div>
	
	<span id="comments_count">
		<%= pluralize(@comments.count, "Comentário") %>
	</span>
	<hr />

	<div id="comments" class="content-box-back-grey">
	  <%= render :partial => @comments, :locals => { :list => true }  %>
	</div>
	
</div>
