<br /><br /><br /><br /><br />
Usuário <%= link_to @user.name, @user %> tem <%= @badges.count %> escudo(s)
<table>
  <tr>
    <th>Badge</th>
    <th>Descrição</th>
  </tr>

<% @badges.each do |badge| %>
  <tr>
    <td><%= badge.badge.name %></td>
    <td><%= badge.badge.description %></td>
  </tr>
<% end %>
</table>

<br />