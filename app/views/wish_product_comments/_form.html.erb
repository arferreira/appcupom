<%= form_for(@wish_product_comment) do |f| %>
  <% if @wish_product_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wish_product_comment.errors.count, "error") %> prohibited this wish_product_comment from being saved:</h2>

      <ul>
      <% @wish_product_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :wish_product %><br />
    <%= f.text_field :wish_product %>
  </div>
  <div class="field">
    <%= f.label :opinion %><br />
    <%= f.text_area :opinion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
