<%= form_for(@recommend) do |f| %>
  <% if @recommend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recommend.errors.count, "error") %> prohibited this partner_comment from being saved:</h2>

      <ul>
      <% @recommend.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<h3>Recomendacao para <%= @partner.company_name%></h3>

  <div class="field">
    <%= f.label :opinion %><br />
    <%= f.text_area :opinion %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>