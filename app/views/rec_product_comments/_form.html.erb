<%= form_for(@rec_product_comment) do |f| %>
  <% if @rec_product_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rec_product_comment.errors.count, "error") %> prohibited this rec_product_comment from being saved:</h2>

      <ul>
      <% @rec_product_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :recommend_product %><br />
    <%= f.text_field :recommend_product %>
  </div>
  <div class="field">
    <%= f.label :opinion %><br />
    <%= f.text_area :opinion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
