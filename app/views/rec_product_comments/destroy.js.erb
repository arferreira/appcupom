/* Eliminate the comment by fading it out */
$('#rec_product_comment_<%= @comment.id %>').slideUp('fast');
/* Replace the count of comments */
$("#comments_count").html("<%= pluralize(@comments_num, 'Comentario') %>");